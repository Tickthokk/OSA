var icon_chooser = {
	open:false,
	init:function() {
		if (icon_chooser.open === true)
			return;

		icon_chooser.open = true;
		
		// Load the Modal Box
		console.log('init!');

		// Load the AJAX

		// Load Events

	}
}